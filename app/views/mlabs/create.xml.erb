<response status="<%= @mlab.errors.empty? ? 'success' : 'error' %>"> 
  <errors>
    <% @mlab.errors.each do |field, message| -%>
      <error><%= field %> <%= message %></error>
    <% end -%>
  </errors>
</response>