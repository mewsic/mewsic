<div class="elements clear-block"> 
  <p class="star">
    <%= rating(person) %>
  </p>
  <p class="thumb">
    <%= link_to avatar_image(person, :medium), user_path(person) %>
  </p>
  <p class="name"><%= link_to truncate(person.login, 10), user_path(person) %></p>
</div>
