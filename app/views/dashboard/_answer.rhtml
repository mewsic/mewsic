<div class="elements clear-block">
  <p class="thumb"><%= link_to avatar_image(answer.user, :icon), user_path(answer.user) %></a></p>
  <div class="text-elements">
    <p class="name"><%= link_to answer.user.login, user_path(answer.user) %></p>
	<p class="abstract"><%= answer.body %></p>
	<p class="abstract align-right"><%= link_to(pluralize(answer.replies_count, 'reply'), answer_path(answer)) %></p>
  </div>
</div>