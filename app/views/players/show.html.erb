<%= javascript_tag %[
	function isLoggerAvailable() {
		var isfb = false; try { isfb = console.log != undefined; } catch( e ) {}; return( isfb );
	}

	var flashvars = {
		serverPath: '',
		soundLength: #@length,
		soundPath: '#@stream',
		wavePath: '#@image'
	};

	var params = { wmode: 'transparent' };
	var attributes = { name: 'playerObject' };
	swfobject.embedSWF( '/Adelao_Myousica_Player.swf', 'myousica_player', '561', '51', '9.0.0', '/expressInstall.swf', flashvars, params, attributes );
] -%>

	<div id="myousica_player"> </div> 
