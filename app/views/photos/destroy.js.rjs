thumb = "gallery-photo-thumb-#{@photo.id}"

page << "myLightWindow._removeLink($('#{thumb}').down('a.lightwindow'));"
page.remove(thumb)
page.visual_effect(:appear, 'no-images-notice') if @user.photos.size.zero?
