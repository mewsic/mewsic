<config>
	<host>http://localhost:3000</host>
	<fms>rtmp://localhost/myousica/</fms>
	<url_request>
		<mylist><%= user_mlabs_path(@user) %></mylist>
		<search>/search/</search>
		<song>/songs/{:id}</song>
		<track>/tracks/{:id}</track>
		<instruments>/instruments.xml?</instruments>
		<genres>/genres.xml</genres>
		<trackediting>/songs/{:id}/edit</trackediting>
		<trackdelete>/tracks/delete/{:id}</trackdelete>
		<trackupload>/tracks</trackupload>
	</url_request>
</config>

